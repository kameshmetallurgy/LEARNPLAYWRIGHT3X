let chip_fail = 10;
let fail_count = 0;
while (chip_fail > fail_count){
    fail_count++
    console.log(`Chip fail count  is ${ fail_count}`);
}
console.log(`The chip has failed as the total count limit of ${fail_count} has reached`);
