let machine_stability_reached = 3;
let attempts = 0;
while ( machine_stability_reached> attempts){
  attempts++;
  console.log(`The total attempts for maintaining the machine is ${attempts}`);

}
console.log(`The machine maintenance attempts of ${attempts}has reached, contact the manager`);